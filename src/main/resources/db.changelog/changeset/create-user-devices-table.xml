<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="create-user-devices-table" author="your-name">
        <createTable tableName="user_devices">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="device_id" type="VARCHAR(255)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="ip_address" type="VARCHAR(45)">
                <constraints nullable="true"/>
            </column>

            <column name="user_agent" type="VARCHAR(500)">
                <constraints nullable="true"/>
            </column>

            <column name="first_login" type="TIMESTAMP">
                <constraints nullable="true"/>
            </column>

            <column name="last_login" type="TIMESTAMP">
                <constraints nullable="true"/>
            </column>

            <column name="is_active" type="BOOLEAN" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Index để tăng tốc query -->
        <createIndex indexName="idx_user_devices_user_id" tableName="user_devices">
            <column name="user_id"/>
        </createIndex>

        <createIndex indexName="idx_user_devices_device_id" tableName="user_devices">
            <column name="device_id"/>
        </createIndex>

        <createIndex indexName="idx_user_devices_user_active" tableName="user_devices">
            <column name="user_id"/>
            <column name="is_active"/>
        </createIndex>

        <!-- Foreign key nếu bạn có bảng users -->
        <addForeignKeyConstraint
                baseTableName="user_devices"
                baseColumnNames="user_id"
                constraintName="fk_user_devices_user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                onDelete="CASCADE"/>
    </changeSet>

</databaseChangeLog>